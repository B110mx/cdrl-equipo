name: CDRL feedback

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify reproducibility contract
        shell: bash
        run: |
          set -euo pipefail
          make setup
          make verify
          make run
      - name: Save verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m02-verification
          path: |
            artifacts/m02-verify.json
            artifacts/m02-run.json
            artifacts/make-verify-output.txt
            artifacts/failure_result.txt
            evidence/m02-relational-model.json
          if-no-files-found: error
