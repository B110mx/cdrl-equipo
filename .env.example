# Configuración local sintética. Nunca reemplaces estos valores por secretos reales en Git.
POSTGRES_DB=cdrl
POSTGRES_USER=cdrl_dev
POSTGRES_PASSWORD=cdrl_dev_only
POSTGRES_PORT=5432
POSTGRES_MIGRATOR_USER=cdrl_migrator_user
POSTGRES_MIGRATOR_PASSWORD=replace-in-local-env
POSTGRES_WRITER_USER=cdrl_writer_user
POSTGRES_WRITER_PASSWORD=replace-in-local-env
POSTGRES_READER_USER=cdrl_reader_user
POSTGRES_READER_PASSWORD=replace-in-local-env
POSTGRES_OPERATOR_USER=cdrl_operator_user
POSTGRES_OPERATOR_PASSWORD=replace-in-local-env
DYNAMODB_PORT=8000
AWS_REGION=us-east-1
